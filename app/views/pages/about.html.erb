<div class="app-card-container">
  <div class="card">
    <h1>About me </h1>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <%= image_tag "profile.png", alt: "Profile image", class: "profile-img" %>
            <div class="skills">
              <% @skills.each do |skill| %>
                  <div class="skill">
                    <h4> <%= skill.title %> </h4>
                    <div class="progress">
                      <div 
                        class="progress-bar progress-bar-striped bg-warning"
                        role="progressbar"
                        aria-valuenow="<%= skill.percent_utilized%>"
                        aria-valuemax="100"
                        style="width: <%= skill.percent_utilized %>%"
                      >
                      </div>
                    </div>
                  </div>
                <% end %>
            </div>
        </div>
      
        <div class="col-md-8">
            <h1><%= current_user.name.capitalize %></h1>
            <p>A website content writer or web content writer is a person who specializes in providing relevant content for websites. Every website has a specific target audience and requires the most relevant content. Content should contain keywords aimed towards improving a website's SEO</p>
            <p>
              <button
                  class="btn btn-primary"
                  type="button"
                  data-toggle="collapse"
                  data-target="#jobHistory"
                  aria-expanded="false"
                  aria-controller="jobHistory"
              >
                  Job History
              </button>      
              <button
                  class="btn btn-primary"
                  type="button"
                  data-toggle="collapse"
                  data-target="#Education"
                  aria-expanded="false"
                  aria-controller="Education"
              >
                  Education
              </button>        
            </p>
            <%= render "jobs" %>
            <%= render "education" %>
        </div>
    </div>
  </div>
</div>
